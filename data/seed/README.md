# Seed Data

Run `pnpm seed` after configuring a local PostgreSQL database. The command pushes the Drizzle schema and loads deterministic synthetic demo data.

The seed script is intentionally deterministic so dashboard metrics, screenshots, smoke tests, and demo walkthroughs stay aligned across machines.
