# Synthetic Cases

TransitionIQ demo cases are inserted by `scripts/src/seed.ts`.

All names, employers, email addresses, coverage dates, plan selections, survey responses, and audit events are synthetic and intended only for the local demo workflow.
