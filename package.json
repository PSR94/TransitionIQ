{
  "name": "transitioniq",
  "version": "0.1.0",
  "description": "Local-first workforce health transition navigator demo for HR, employees, consultants, and platform admins.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/PSR94/TransitionIQ.git"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "pnpm --parallel --filter @workspace/api-server --filter @workspace/transitioniq run dev",
    "seed": "pnpm --filter @workspace/db run push && pnpm --filter @workspace/scripts run seed",
    "db:push": "pnpm --filter @workspace/db run push",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "vitest run",
    "audit:repo": "bash tools/verify_no_ai_traces.sh && bash tools/check_empty_folders.sh",
    "package": "bash tools/package_project.sh",
    "ci": "pnpm typecheck && pnpm test && pnpm build && pnpm audit:repo"
  },
  "private": true,
  "devDependencies": {
    "@types/supertest": "^6.0.3",
    "dotenv-cli": "^8.0.0",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "supertest": "^7.1.4",
    "vitest": "^4.0.15"
  }
}
